name: Release workflow trigger

on:
  issue_comment:
    types: [created]

jobs:
  release:
    if: ${{!github.event.issue.pull_request && github.event.comment.body == '/start' && startsWith(github.event.issue.title, '🎤 [Release]') }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run release workflow
        run: |
          echo "Release workflow started 🎉"
